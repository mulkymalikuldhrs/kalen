{
  "name": "@kalen/identity",
  "version": "0.1.0",
  "description": "KALEN identity service — WebAuthn, agent identity, RBAC, and JWT token management",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "license": "AGPL-3.0",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@kalen/shared": "workspace:*",
    "@simplewebauthn/server": "^13.0.0",
    "jose": "^5.9.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
