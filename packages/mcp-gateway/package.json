{
  "name": "@kalen/mcp-gateway",
  "version": "0.1.0",
  "description": "KALEN MCP Protocol Gateway — tool discovery, routing, and governance",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "license": "AGPL-3.0",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "test": "jest --config jest.config.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@kalen/shared": "workspace:*",
    "@kalen/identity": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "jest": "^30.0.0",
    "ts-jest": "^29.0.0",
    "@types/jest": "^30.0.0"
  }
}
